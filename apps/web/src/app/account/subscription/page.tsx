import AccountPlaceholder from '@/components/AccountPlaceholder'

export default function SubscriptionPage() {
  return <AccountPlaceholder message="Subscription management is on its way. Check back soon." />
}
